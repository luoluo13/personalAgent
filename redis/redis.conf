# 基础配置
bind 0.0.0.0
port 6379
protected-mode no

# 持久化配置（重要！防止数据丢失）
save 900 1
save 300 10
save 60 10000

# 数据文件
dbfilename dump.rdb
dir /data

# 日志
loglevel notice

# 内存上限（根据你的服务器调整，本地测试可以小点）
maxmemory 256mb
maxmemory-policy allkeys-lru